import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  tabBar: {
    flexDirection: "row",
    paddingVertical: 10,
    height: 100,
    backgroundColor: "#363636",
  },
})
